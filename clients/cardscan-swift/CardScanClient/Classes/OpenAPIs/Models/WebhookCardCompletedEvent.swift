//
// WebhookCardCompletedEvent.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Triggered after a successful insurance card scan */
public struct WebhookCardCompletedEvent: Codable, JSONEncodable, Hashable {

    /** Unique identifier for the created card. */
    public var cardId: UUID
    public var configuration: WebhookCardCompletedEventConfiguration
    /** Timestamp for when the card was created. */
    public var createdAt: Date
    /** Flag indicating whether the card is deleted. */
    public var deleted: Bool
    /** Unique identifier for the session. */
    public var sessionId: String
    /** Type of event. */
    public var type: String
    /** Timestamp for the last update to the card record. */
    public var updatedAt: Date
    /** Identifier for the user associated with the event. */
    public var userId: String

    public init(cardId: UUID, configuration: WebhookCardCompletedEventConfiguration, createdAt: Date, deleted: Bool, sessionId: String, type: String, updatedAt: Date, userId: String) {
        self.cardId = cardId
        self.configuration = configuration
        self.createdAt = createdAt
        self.deleted = deleted
        self.sessionId = sessionId
        self.type = type
        self.updatedAt = updatedAt
        self.userId = userId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case cardId = "card_id"
        case configuration
        case createdAt = "created_at"
        case deleted
        case sessionId = "session_id"
        case type
        case updatedAt = "updated_at"
        case userId = "user_id"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(cardId, forKey: .cardId)
        try container.encode(configuration, forKey: .configuration)
        try container.encode(createdAt, forKey: .createdAt)
        try container.encode(deleted, forKey: .deleted)
        try container.encode(sessionId, forKey: .sessionId)
        try container.encode(type, forKey: .type)
        try container.encode(updatedAt, forKey: .updatedAt)
        try container.encode(userId, forKey: .userId)
    }
}

