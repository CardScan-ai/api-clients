//
// GenerateCardUploadUrl200Response.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct GenerateCardUploadUrl200Response: Codable, JSONEncodable, Hashable {

    public var cardId: UUID
    public var scanId: UUID
    /** The URL to upload the card image. */
    public var uploadUrl: String
    public var uploadParameters: UploadParameters

    public init(cardId: UUID, scanId: UUID, uploadUrl: String, uploadParameters: UploadParameters) {
        self.cardId = cardId
        self.scanId = scanId
        self.uploadUrl = uploadUrl
        self.uploadParameters = uploadParameters
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case cardId = "card_id"
        case scanId = "scan_id"
        case uploadUrl = "upload_url"
        case uploadParameters = "upload_parameters"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(cardId, forKey: .cardId)
        try container.encode(scanId, forKey: .scanId)
        try container.encode(uploadUrl, forKey: .uploadUrl)
        try container.encode(uploadParameters, forKey: .uploadParameters)
    }
}

